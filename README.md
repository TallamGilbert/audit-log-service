# Immutable Audit Log Service

## What This Service Does

A write-only REST API that records audit events into a permanent, queryable record.

## Setup & Configuration

```bash
npm install
cp .env.example .env
# Edit .env with your configuration
```
